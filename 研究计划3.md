# 永久投资组合研究 · 第三阶段：最优权重探索

> 前两阶段已验证了"25%等权永久组合"的稳健性。第三阶段回答：
> **不同的股票引擎，应该搭配怎样的债券/黄金/现金比例，才是最优的？**
> 
> 沿用Phase 1方法：**每个权重组合，跑每个交易日入场持有至2026-06-06**

---

## 一、研究动因

第一阶段，三个指数的25%等权结果：

| 组别    | 累计收益    | 年化    | 回撤        | 24月胜率     |
| ----- |:-------:|:-----:|:---------:|:---------:|
| 标普500 | +168.8% | 8.4%  | 15.4%     | 92.9%     |
| 纳指100 | +174.9% | 10.2% | 20.5%     | —         |
| 红利低波  | +80.9%  | 7.2%  | **9.23%** | **91.7%** |
| 日经225 | +146.8% | 9.0%  | **11.8%** | —         |

三者的风险收益特征差异巨大，25%等权很可能不是各自的最优解。

---

## 二、优化对象（四版本）

| 版本          | 代码            | 股票资产            | 债券      | 黄金        | 现金   | 数据来源                  |
|:-----------:|:-------------:|:--------------- |:------- |:--------- |:---- |:--------------------- |
| **A-红利低波**  | hongli_lowvol | 红利低波 H30269.CSI | 固定3%    | 沪金 AU.SHF | 2%   | `cn_H30269_CSI.csv`   |
| **B-标普500** | sp500         | 标普500 ^SP500TR  | TLT ETF | COMEX金    | 2%   | `us_sp500_daily.csv`  |
| **C-纳指100** | nasdaq        | 纳指100 ^NDXT     | TLT ETF | COMEX金    | 2%   | `us_nasdaq_daily.csv` |
| **D-日经225** | nikkei225     | 日经225 ^N225     | 固定1.5%  | GOLD_USD  | 0.5% | 已有日经回测数据              |

**约束条件**（每类资产最低5%，避免极端配置）：

| 资产  | 最小权重 | 最大权重 | 步长  |
|:---:|:----:|:----:|:---:|
| 股票  | 10%  | 50%  | 5%  |
| 债券  | 5%   | 50%  | 5%  |
| 黄金  | 5%   | 40%  | 5%  |
| 现金  | 5%   | 30%  | 5%  |

四者和=100%，每版本约 **300~400** 个有效组合。

---

## 三、优化方法（同步Phase 1）

### 3.1 核心算法

沿用Phase 1的 `backtest_single_entry` 方法，但参数化权重：

```
对 每个版本、每个权重组合 (w_stock, w_bond, w_gold, w_cash):
  对 每个交易日 entry_date（从数据起始到2026-06-06前）:
    pv = 初始资金（1元）
    w = [w_stock, w_bond, w_gold, w_cash]
    对 每一天从 entry_date 到 2026-06-06:
      计算当日4资产收益率
      pv *= (1 + Σ(w × r))
      w = w × (1+r) / Σ(w × (1+r))
      若偏离阈值>±8% 或 每年1月触发再平衡:
        w = [w_stock, w_bond, w_gold, w_cash]
        pv *= (1 - 0.001)  # 交易成本
    记录: {entry_date, final_return, CAGR, MaxDD, Sharpe, Calmar}
  汇总所有入场点的_mean、_median、极端值、胜率
  保存到结果CSV
```

### 3.2 计算效率方案

**纯Python循环太慢**，必须用 **numpy向量化批量处理**。

关键技术：

1. 预计算全部4资产日收益率矩阵（T × 4）
2. 对每个权重组合，用一个 n×4 的权重矩阵同时跟踪所有入场点
3. weight_update、rebalance_check 全部用 numpy 矩阵运算，不用循环

```
# 单权重组合的向量化伪代码
returns = precomputed_returns  # shape (T, 4)
w_target = np.array([ws, wb, wg, wc])
n = T  # 入场数 = 交易日数

pv = np.ones(n)     # 每个入场点的当前净值
w = np.tile(w_target, (n, 1))  # 每个入场点的当前权重 (n,4)

for d in range(T-1):
    r_d = returns[d+1] / returns[d] - 1  # (4,) 当日收益率
    # 只有 d>=entry_date 的入场点才更新（用mask控制）
    active = np.arange(n) <= d
    # 更新净值
    pv_ret = np.sum(w[active] * r_d, axis=1)
    pv[active] *= (1 + pv_ret)
    # 更新权重
    w_active = w[active] * (1 + r_d)
    w_active /= np.sum(w_active, axis=1, keepdims=True)
    w[active] = w_active
    # 再平衡检查（向量化）
    dev = np.abs(w[active] - w_target).max(axis=1)
    rebal = dev > 0.08
    if rebal.any():
        w[active][rebal] = w_target
        pv[active][rebal] *= 0.999
    # 年线再平衡

# 结果：pv 即为每个入场点持有至今日的终值
```

### 3.3 计算量预估

| 版本    | 交易日数   | 有效权重组合 | 内层步数                  | 预估运行时间       |
|:-----:|:------:|:------:|:---------------------:|:------------:|
| 红利低波  | ~4,200 | ~350   | 4,200×4,200×350≈6.2B  | **~20-30分钟** |
| 标普500 | ~5,800 | ~350   | 5,800×5,800×350≈11.8B | **~40-60分钟** |
| 纳指100 | ~5,800 | ~350   | 同标普≈11.8B             | **~40-60分钟** |
| 日经225 | ~5,800 | ~350   | 同标普≈11.8B             | **~40-60分钟** |

**合计：约2.5~3.5小时（四版本并行 ≈ 1小时总耗时）**

---

## 四、并行执行策略

利用 Hermes `delegate_task` 分三层并行：

### 第1层：四版本并行（delegate_task × 4）

```
┌─────────────────────────────────────────────────┐
│                   主Agent                        │
│    → 编写优化引擎脚本                           │
│    → 分派4个子Agent并行跑四个版本               │
└──────┬──────────┬──────────┬──────────┬────────┘
       │          │          │          │
       ▼          ▼          ▼          ▼
  Agent-A     Agent-B    Agent-C    Agent-D
 (红利低波)   (标普500)  (纳指100)  (日经225)
```

### 第2层：每个版本内部再分块并行

每个版本约350个权重组合，按每批35组切分**10批并行**：

```
Agent-A (红利低波)
  ┌─── 批1: 10-15%股票(约35组) ── → result1.csv
  ┌─── 批2: 20%股票(约35组) ──── → result2.csv
  ┌─── ... 共10批并行 ────────── → result10.csv
  → merge + 汇总 → grid_hongli_lowvol.csv
```

这样每批计算量降到约1/10，10批并行≈单批时间。

### 第3层：结果汇总

所有版本跑完后，由主Agent汇总合并：

- 提取每个版本的帕累托前沿
- 计算三档推荐配置
- 生成对比图表
- 撰写分析报告

---

## 五、优化目标与推荐输出

### 5.1 评价指标

每个权重组合计算**全场入场的汇总统计**：

| 指标               | 计算方式          | 优化方向         |
|:---------------- |:------------- |:------------:|
| **mean_CAGR**    | 所有入场点年化收益均值   | ↑            |
| **median_CAGR**  | 所有入场点年化收益中位数  | ↑            |
| **CAGR_p90**     | 第90百分位收益（好运气） | ↑            |
| **CAGR_p10**     | 第10百分位收益（坏运气） | ↑（越高越好，下限保护） |
| **mean_MaxDD**   | 所有入场点最大回撤均值   | ↓            |
| **worst_MaxDD**  | 所有入场点最大回撤的最大值 | ↓            |
| **mean_Sharpe**  | 所有入场点夏普均值     | ↑            |
| **mean_Calmar**  | 所有入场点卡尔玛均值    | ↑            |
| **win_rate_24m** | 24月持有胜率       | ↑            |
| **worst_return** | 最差入场点的最终收益    | ↑（必须>0）      |

### 5.2 三档推荐

| 档位      | 定位                 | 优先排序依据                              |
|:-------:|:------------------ |:----------------------------------- |
| **稳健型** | 最差入场点回撤最小 + 最差收益为正 | 先筛 worst_return>0，再排 worst_MaxDD 最小 |
| **均衡型** | 卡尔玛比率最高（收益/回撤性价比）  | mean_Calmar 最高                      |
| **进取型** | 收益最高，回撤≤25%        | mean_CAGR 最高，约束 worst_MaxDD ≤ 25%   |

---

## 六、输出文件

```
永久投资组合研究/
├── 研究计划3.md                           # 本文件
├── results3/                              # 第三阶段结果
│   ├── hongli_lowvol/                    # 红利低波版
│   │   ├── grid_results.csv             # 全网格汇总（每行一个权重组合）
│   │   └── top_recommendations.csv      # 三档推荐 + 前十候选
│   ├── sp500/
│   │   ├── grid_results.csv
│   │   └── top_recommendations.csv
│   ├── nasdaq/
│   │   ├── grid_results.csv
│   │   └── top_recommendations.csv
│   ├── nikkei225/
│   │   ├── grid_results.csv
│   │   └── top_recommendations.csv
│   ├── optimal_summary.csv               # 四版本三档推荐汇总
│   └── pareto_frontiers.csv              # 四版本帕累托前沿点
├── charts3/
│   ├── frontier_all_versions.png         # 四版本帕累托前沿对比
│   ├── calmar_heatmap_4panel.png         # 四版卡尔玛热力图（2×2布局）
│   ├── sensitivity_stock_weight.png      # 股票权重±波动对收益/回撤的影响
│   └── optimal_allocation_bars.png       # 三档推荐配置柱状图
├── reports/
│   ├── 最优权重分析报告.md               # 完整分析报告
│   └── 最优权重推荐摘要.md               # 精炼版（核心推荐 + 实操建议）
└── scripts/
    ├── optimize_weights.py               # 权重优化引擎（本阶段核心）
    └── merge_batches.py                  # 批次结果合并 + 汇总
```

---

## 七、grid_results.csv 格式

每行一个权重组合，字段：

```
w_stock,w_bond,w_gold,w_cash,
entry_count,                          # 入场点数量
mean_cagr,median_cagr,cagr_p10,cagr_p90,
mean_maxdd,worst_maxdd,
mean_sharpe,mean_calmar,
win_rate_1m,win_rate_3m,win_rate_6m,win_rate_12m,win_rate_24m,
best_return,worst_return,             # 最幸运和最倒霉入场点的累计收益
all_positive_flag                     # 是否所有入场点均为正收益
```

---

## 八、执行步骤

| 步骤     | 内容                               | 执行     | 预估时间         |
|:------:|:-------------------------------- |:------ |:------------:|
| 1      | 编写 `optimize_weights.py` 向量化优化引擎 | 主Agent | 30分钟         |
| 2      | 并行分发：4版本 × 每版内10批次并行             | 子Agent | **30~60分钟**  |
| 3      | 合并批次结果 + 帕累托前沿提取                 | 主Agent | 5分钟          |
| 4      | 四版卡尔玛热力图 + 帕累托前沿对比图              | 子Agent | 10分钟         |
| 5      | 撰写分析报告                           | 子Agent | 20分钟         |
| 6      | 更新 README_ZH.md / README.md      | 主Agent | 10分钟         |
| **合计** |                                  |        | **约1.5~2小时** |

---

## 九、风险与注意事项

1. **过拟合风险**：历史最优权重不一定等于未来最优
   - ✅ 应对：输出帕累托前沿（多个合理选项），而非单一"精确解"
2. **日经版债券**：固定1.5%是近似，日本实际长期国债利率长期接近0%
3. **再平衡规则一致性**：与Phase 1完全一致（±8%阈值 + 年线）
4. **向量化计算**：用numpy矩阵运算取代逐入场点循环，大幅提升速度

---

## 十、验证方案

对最终推荐的每版三档配置，额外验证：

| 验证项           | 方法                      |
|:------------- |:----------------------- |
| **比25%等权好吗？** | 推荐配置 vs 25%等权，各项指标对比    |
| **入场时点韧性**    | 按市场周期分段（牛/熊/震荡），看收益是否稳定 |
| **最差情况**      | 最倒霉的入场日收益是否为正           |

---

*研究计划3版本：v1.1（已更新：增加日经225、向量化并行方案）*
*创建日期：2026-06-07*
*状态：待确认*
